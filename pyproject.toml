# ###############
# UV
# ###############
[project]
name = "deeead"
version = "0.1.0"
description = "Does Everything Already Done"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["dead", "dead-assets"]

[tool.uv.sources]
dead = { workspace = true }
dead-assets = { workspace = true }

[tool.uv.workspace]
members = ["packages/*"]
