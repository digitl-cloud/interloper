// Catalog is loaded in the auth middleware after authentication.
// This plugin is intentionally empty — kept as a placeholder for any
// future client-only specs initialization that doesn't depend on auth.
export default defineNuxtPlugin(() => {})
